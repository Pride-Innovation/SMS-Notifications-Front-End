const HOME: string = "/"
const SMS_LOGS: string = "/sms-logs";
const REPORTS: string = "/dashboard";
const BIRTHDAY_REPORTS: string = `${REPORTS}/birthdays`;
const UPLOADS: string = `${REPORTS}/uploads`

export const ROUTES = {
    SMS_LOGS,
    HOME,
    REPORTS,
    UPLOADS,
    BIRTHDAY_REPORTS
}